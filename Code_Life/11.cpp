/*
#include <iostream>

using namespace std;

int main() {


    string str;
    cin >> str;

    for (decltype(str.size()) index = 0; index != str.size() && !isupper(str[index]); ++index) {

        str[index] = toupper(str[index]);

        cout << index << endl;

    }

    cout << str <<

    return 0;
}
*/
